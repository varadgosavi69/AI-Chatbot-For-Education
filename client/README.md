# QuickDoubt — Client

React + TypeScript + Vite frontend for QuickDoubt, an AI-powered educational chatbot.

## Development

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies `/api` requests to the backend on port 3001.
